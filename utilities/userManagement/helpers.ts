/**
 * @description A function which parses a submitted password and returns whether or not it is valid. 
 * @param password 
 * @returns 
 */
export function isPasswordOk(password: string) {
    // TODO replace with password validation logic. 
    return true
}